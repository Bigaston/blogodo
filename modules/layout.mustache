<!DOCTYPE html>
<html>
	<head>
		<title>{{ title }}</title>
		<link rel="stylesheet" type="text/css" href="/static/style.css">
	</head>

	<body>
		<div class="wrapper">
			<img class="header-img" src="{{{ img }}}" alt="Bannière" />
			<div class="content">
				<h1>{{ title }}</h1>
				<p class="subtitle">{{ subtitle }}</p>
				<p class="tags">Type : 
					{{#tags}}
						<span>{{name}}</span>
					{{/tags}}
				</p>

				{{{ contents}}}
				<p class="date">{{ pubdate }}</p>
			</div>
		</div>
	</body>
</html>
